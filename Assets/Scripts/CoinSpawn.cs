using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoinSpawn : MonoBehaviour
{
    //public Transform StratPos;
    //public Transform EndPos;

    //private float Current_coin_position;
    //// Start is called before the first frame update
    //void Start()
    //{
    //    Current_coin_position = StratPos.position.z;
    //    while (Current_coin_position < EndPos.position.z)
    //    {
    //        GameObject Coin = ObjectPool.SharedInstance.GetPooledObject();
    //        if (Coin != null)
    //        {
    //            Coin.transform.position = new Vector3(StratPos.position.x, StratPos.position.y + 1f, Current_coin_position);
    //            Coin.SetActive(true);
    //        }
    //        Current_coin_position++;

    //        //Current_coin_position = Current_coin_position + 2f;
    //        Debug.Log(Current_coin_position);
    //    }

    //}
}
